<h1>Articles</h1>

<%= link_to 'New Article', new_article_path %>


<% @articles.each do |article| %>
  <h2><%= link_to article.title, article_path(article) %></h2>
  <p><%= article.title[0..50] %>..</p>
  <p><%= link_to 'Edit', edit_article_path(article) %></p>
<% end %>
